# 前端编码规范与最佳实践

## 文档说明

本文档基于 **Ant Design 5.19.3** 和 **Ant Design Pro** 制定，旨在指导开发者编写高质量、可维护、可扩展的前端代码。

**核心内容**：
- **主题适配**：亮色/暗色/紧凑模式自动适配
- **组件规范**：ProComponents 使用规范和最佳实践
- **现代化重构**：Flex 布局等现代化组件迁移指南
- **代码质量**：编码规范、检查清单、常见问题

---

## 一、核心开发原则

### 基本原则

> **规范的代码可以自动适配主题，不规范的代码会让维护工作量增加数十倍甚至上百倍。**

在开发过程中，请始终遵循以下核心原则：

1. **使用 Less 模块**组织样式，避免内联样式
2. **使用 theme.useToken()**处理动态样式
3. **使用 CSS 变量**而非硬编码颜色（如 `#fff`、`#262626`）
4. **使用 useApp() hook**而非静态方法（message/modal/notification）
5. **使用 ProForm 组件的 formRef**而非 Form.useForm()
6. **不修改 ProComponents** 的样式 props（会导致级联影响）
7. **设计阶段就考虑主题适配**，而非事后补救

### ProComponents 特别注意

> **ProComponents 组件会自动适配主题，不要添加显式的样式属性。**

**禁止操作**：
```typescript
// ❌ 不要这样做
<ProCard
  style={{ background: 'var(--ant-color-bg-container)' }}
  headStyle={{ background: 'var(--ant-color-bg-container)' }}
  bodyStyle={{ padding: 16 }}
/>
```

**原因**：
- ProComponents 内部使用全局状态或 context 管理主题
- 显式设置样式可能干扰这个机制
- 可能导致其他页面出现级联影响

**正确做法**：
```typescript
// ✅ 让组件自动适配主题
<ProCard>
  <ProTable />
</ProCard>

<PageContainer>
  内容
</PageContainer>
```

---

## 二、Ant Design 5 主题系统概述

### 核心特性

Ant Design 5 引入了全新的主题系统，提供以下特性：

1. **Design Token 体系**：基于设计令牌的主题定制
2. **CSS-in-JS**：完整的 CSS 变量支持
3. **动态主题切换**：运行时无缝切换主题
4. **TypeScript 类型支持**：完整的类型提示
5. **预设算法**：默认、暗色、紧凑三种算法

### 三种预设算法

```typescript
import { theme } from 'antd';

// 1. 默认算法（亮色主题）
theme.defaultAlgorithm

// 2. 暗色算法
theme.darkAlgorithm

// 3. 紧凑算法（更小的组件尺寸）
theme.compactAlgorithm

// 可以组合使用
[theme.darkAlgorithm, theme.compactAlgorithm] // 暗色 + 紧凑
```

### 主题变量层级

```typescript
const { token } = theme.useToken();

// token 对象包含所有主题变量：
token.colorPrimary      // 主题色
token.colorSuccess      // 成功色
token.colorWarning      // 警告色
token.colorError        // 错误色
token.colorInfo         // 信息色
token.colorText         // 主文本色
token.colorTextSecondary // 次要文本色
token.colorTextTertiary  // 第三级文本色
token.colorBorder        // 边框色
token.colorBgContainer   // 容器背景色
token.colorBgLayout      // 布局背景色
// ... 更多
```

---

## 三、规范的开发方式

### ✅ 方式一：使用 theme.useToken() Hook（推荐）

**适用场景**：需要动态计算样式或根据状态改变颜色时

```typescript
import { theme } from 'antd';

const MyComponent: React.FC = () => {
  const { token } = theme.useToken();

  // 动态颜色计算
  const getStatusColor = (status: string) => {
    switch (status) {
      case 'success': return token.colorSuccess;
      case 'warning': return token.colorWarning;
      case 'error': return token.colorError;
      default: return token.colorTextSecondary;
    }
  };

  return (
    <div style={{ color: getStatusColor(currentStatus) }}>
      {content}
    </div>
  );
};
```

**优点**：
- ✅ 完整的 TypeScript 类型支持
- ✅ 自动跟随主题变化
- ✅ 适合动态计算场景

---

### ✅ 方式二：使用 Less 模块 + CSS 变量（推荐）

**适用场景**：静态样式定义

#### 第一步：创建 Less 文件

```less
// Component.less
.container {
  background: var(--ant-color-bg-container);
  border: 1px solid var(--ant-color-border);
  padding: 16px;
}

.title {
  font-size: 14px;
  font-weight: 500;
  color: var(--ant-color-text);
}

.description {
  font-size: 12px;
  color: var(--ant-color-text-secondary);
}

// 状态样式
.status {
  &.success {
    color: var(--ant-color-success);
  }

  &.warning {
    color: var(--ant-color-warning);
  }

  &.error {
    color: var(--ant-color-error);
  }
}
```

#### 第二步：在组件中使用

```typescript
import styles from './Component.less';
import classNames from 'classnames';

const MyComponent: React.FC = () => {
  return (
    <div className={styles.container}>
      <div className={styles.title}>标题</div>
      <div className={styles.description}>描述文本</div>
      <div className={classNames(styles.status, styles.success)}>
        成功状态
      </div>
    </div>
  );
};
```

**优点**：
- ✅ 样式代码集中管理
- ✅ 可复用性强
- ✅ 利用 CSS 缓存优化性能
- ✅ 支持嵌套和选择器

---

### ✅ 方式三：结合使用（最佳实践）

**静态样式用 Less，动态样式用 token**

```typescript
import { theme } from 'antd';
import classNames from 'classnames';
import styles from './Component.less';

const MyComponent: React.FC = () => {
  const { token } = theme.useToken();

  return (
    <div className={styles.container}>
      {/* 静态样式使用 Less */}
      <div className={styles.title}>标题</div>

      {/* 动态样式使用 token */}
      <div style={{ color: isActive ? token.colorPrimary : token.colorTextSecondary }}>
        {isActive ? '激活' : '未激活'}
      </div>
    </div>
  );
};
```

---

### ✅ 方式四：使用 Flex 组件替代 Row/Col（现代化布局）

**状态**：⏳ 待启动 - 可渐进式迁移 Row/Col 布局

**适用场景**：需要灵活布局的场景，替代传统的 Row/Col 栅格系统

#### 为什么使用 Flex 组件？

**优势说明**：
1. **更简洁的 API**：直接使用 flexbox 属性，无需计算栅格数（span）
2. **更好的响应式支持**：自动适配容器大小，无需手动设置断点
3. **更直观的代码**：CSS flexbox 语义化属性，易读易维护
4. **更小的包体积**：Ant Design 5 原生组件，无额外依赖
5. **更好的主题适配**：完美集成 Design Token 系统

#### 迁移指南

**迁移前（Row/Col）**：
```typescript
import { Row, Col } from 'antd';

<Row gutter={16}>
  <Col span={8}>
    <div>左侧内容</div>
  </Col>
  <Col span={16}>
    <div>右侧内容</div>
  </Col>
</Row>

<Row gutter={[16, 16]} justify="space-between" align="middle">
  <Col span={12}>项目 1</Col>
  <Col span={12}>项目 2</Col>
</Row>
```

**迁移后（Flex）**：
```typescript
import { Flex } from 'antd';

<Flex gap={16}>
  <div style={{ flex: '0 0 33.33%' }}>左侧内容</div>
  <div style={{ flex: '0 0 66.67%' }}>右侧内容</div>
</Flex>

<Flex gap={16} justify="space-between" align="center" wrap="wrap">
  <div>项目 1</div>
  <div>项目 2</div>
</Flex>
```

#### 常用属性对照表

| Row/Col 属性 | Flex 属性 | 说明 |
|-------------|----------|------|
| `gutter={16}` | `gap={16}` | 间距设置 |
| `gutter={[16, 8]}` | `gap="16px 8px"` | 水平和垂直间距 |
| `justify="start"` | `justify="flex-start"` | 主轴对齐 |
| `align="top"` | `align="flex-start"` | 交叉轴对齐 |
| `Col span={12}` | `style={{ flex: '0 0 50%' }}` | 固定宽度比例 |
| - | `flex={1}` | 自动填充剩余空间 |
| - | `vertical` | 垂直布局 |
| - | `wrap="wrap"` | 自动换行 |

#### 实际应用示例

**示例 1：表单操作栏**
```typescript
// ✅ 使用 Flex
<Flex justify="flex-end" gap={8}>
  <Button>取消</Button>
  <Button type="primary">确定</Button>
</Flex>
```

**示例 2：卡片头部**
```typescript
// ✅ 使用 Flex
<Flex justify="space-between" align="center">
  <div>
    <h3>标题</h3>
    <p>描述信息</p>
  </div>
  <Button>操作</Button>
</Flex>
```

**示例 3：响应式网格**
```typescript
// ✅ 使用 Flex
<Flex gap={16} wrap="wrap">
  {items.map(item => (
    <div key={item.id} style={{ flex: '0 0 calc(25% - 12px)' }}>
      <Card>{item.content}</Card>
    </div>
  ))}
</Flex>
```

#### 迁移检查清单

**计划阶段**：
- [ ] 识别项目中所有使用 Row/Col 的布局
- [ ] 评估迁移优先级（新页面优先，复杂布局可延后）
- [ ] 确认不影响现有功能

**开发阶段**：
- [ ] 替换 `<Row>` 为 `<Flex>`
- [ ] 将 `gutter` 改为 `gap`
- [ ] 移除 `<Col>` 包装，直接使用内容元素
- [ ] 使用 `flex` 样式属性替代 `span` 计算
- [ ] 调整对齐属性（justify/align）

**测试阶段**：
- [ ] 验证不同屏幕尺寸下的布局效果
- [ ] 测试亮色/暗色主题下的显示
- [ ] 确认间距和对齐符合设计要求
- [ ] 检查浏览器兼容性（现代浏览器均支持）

**优点**：
- ✅ 代码更简洁直观
- ✅ 减少 DOM 层级（少一层 Col 包装）
- ✅ 更好的性能（原生 CSS flexbox）
- ✅ 完美的主题适配

---

### ✅ 方式五：使用 Splitter 分隔面板（可拖拽布局）

#### 基本用法

使用 `Splitter` 替代 `ProCard split="vertical"` 实现可拖拽的分栏布局：

```tsx
import { Splitter } from "antd";
import useResponsiveLayout from "@/hooks/useResponsiveLayout";

const MyPage: React.FC = () => {
  const layout = useResponsiveLayout({ breakpoint: "xl" });

  return (
    <Splitter layout={layout} className={styles.splitterContainer}>
      <Splitter.Panel
        defaultSize="25%"
        min={180}
        max="45%"
        collapsible={{ start: true }}
      >
        <ProCard title="左侧">内容</ProCard>
      </Splitter.Panel>
      <Splitter.Panel min={600} max="80%">
        <ProCard>右侧</ProCard>
      </Splitter.Panel>
    </Splitter>
  );
};
```

**Less 样式**：
```less
.splitterContainer {
  height: calc(100vh - 200px);
  min-height: 600px;

  .leftPanel,
  .rightPanel {
    display: flex;
    flex-direction: column;
    height: 100%;

    :global(.ant-pro-card) {
      display: flex;
      flex-direction: column;
      height: 100%;
      border: none;

      :global(.ant-pro-card-body) {
        flex: 1;
        overflow: auto;
      }
    }
  }
}
```

#### 参数推荐值

| 场景 | 左侧 min | 左侧 defaultSize | 右侧 min | 断点 |
|------|----------|------------------|----------|------|
| 标准组织树 | 180 | 25% | 600 | xl |
| 深层级组织树 | 380 | 30% | 600 | xl |
| 三栏布局 | 180 | 20% | 480/520 | xxl |

**重要**：
- `min` 和 `max` 使用数字时表示像素（如 `min={180}`），不要用字符串（如 `min="180px"`）
- 百分比用字符串（如 `max="45%"`）
- 应用导航栏在 < 1200px 自动收起（已在 `app.tsx` 配置）

---

## 四、常用 CSS 变量速查表

| 用途 | CSS 变量 | Token 属性 |
|-----|---------|-----------|
| **背景色** |
| 容器背景 | `var(--ant-color-bg-container)` | `token.colorBgContainer` |
| 页面背景 | `var(--ant-color-bg-layout)` | `token.colorBgLayout` |
| 悬浮层背景 | `var(--ant-color-bg-elevated)` | `token.colorBgElevated` |
| 填充色（最浅） | `var(--ant-color-fill-quaternary)` | `token.colorFillQuaternary` |
| 填充色（浅） | `var(--ant-color-fill-tertiary)` | `token.colorFillTertiary` |
| 填充色（中） | `var(--ant-color-fill-secondary)` | `token.colorFillSecondary` |
| 填充色（深） | `var(--ant-color-fill)` | `token.colorFill` |
| **文字色** |
| 主文本 | `var(--ant-color-text)` | `token.colorText` |
| 次要文本 | `var(--ant-color-text-secondary)` | `token.colorTextSecondary` |
| 第三级文本 | `var(--ant-color-text-tertiary)` | `token.colorTextTertiary` |
| 禁用文本 | `var(--ant-color-text-disabled)` | `token.colorTextDisabled` |
| **边框色** |
| 主边框 | `var(--ant-color-border)` | `token.colorBorder` |
| 次级边框 | `var(--ant-color-border-secondary)` | `token.colorBorderSecondary` |
| **功能色** |
| 主题色 | `var(--ant-color-primary)` | `token.colorPrimary` |
| 主题色悬停 | `var(--ant-color-primary-hover)` | `token.colorPrimaryHover` |
| 主题色激活 | `var(--ant-color-primary-active)` | `token.colorPrimaryActive` |
| 主题色背景 | `var(--ant-color-primary-bg)` | `token.colorPrimaryBg` |
| 主题色边框 | `var(--ant-color-primary-border)` | `token.colorPrimaryBorder` |
| 成功色 | `var(--ant-color-success)` | `token.colorSuccess` |
| 成功色悬停 | `var(--ant-color-success-hover)` | `token.colorSuccessHover` |
| 警告色 | `var(--ant-color-warning)` | `token.colorWarning` |
| 警告色悬停 | `var(--ant-color-warning-hover)` | `token.colorWarningHover` |
| 错误色 | `var(--ant-color-error)` | `token.colorError` |
| 错误色悬停 | `var(--ant-color-error-hover)` | `token.colorErrorHover` |
| 信息色 | `var(--ant-color-info)` | `token.colorInfo` |

---

## 五、不规范的代码示例

### ❌ 错误示例 1：硬编码颜色值

```typescript
// ❌ 不要这样做
<div style={{
  backgroundColor: '#fff',
  color: '#262626',
  border: '1px solid #d9d9d9'
}}>
```

**问题**：
- 主题切换时无法自动适配
- 需要手动查找替换
- 维护成本高

### ❌ 错误示例 2：过度使用内联样式

```typescript
// ❌ 不要这样做
<div style={{ padding: '16px', margin: '8px', backgroundColor: '#fafafa' }}>
  <div style={{ fontSize: '14px', fontWeight: 500, color: '#262626' }}>
    标题
  </div>
  <div style={{ fontSize: '12px', color: '#8c8c8c' }}>
    描述
  </div>
</div>
```

**问题**：
- 样式分散，难以维护
- 无法复用
- 代码可读性差

### ❌ 错误示例 3：修改 ProComponents 的样式 props

```typescript
// ❌ 不要这样做
<ProCard
  style={{ background: 'var(--ant-color-bg-container)' }}
  headStyle={{ background: 'var(--ant-color-bg-container)' }}
  bodyStyle={{ padding: 16 }}
/>
```

**问题**：
- 破坏了 ProComponents 的自动主题适配机制
- 可能导致其他页面的级联影响
- ProCard 会自动适配主题，不需要显式设置

---

## 六、开发检查清单

### 样式规范检查

**编码阶段**：
- [ ] **不使用硬编码颜色值**（如 `#fff`、`#262626` 等）
- [ ] **优先使用 Less 模块**而非内联样式
- [ ] **动态样式使用 `theme.useToken()`**
- [ ] **使用语义化的 CSS 类名**

**组件使用检查**：
- [ ] **不修改 ProComponents 的样式 props**（style/headStyle/bodyStyle 等）
- [ ] **ProCard 操作按钮使用 `extra` 而非内部工具栏**（标题与按钮同行）
- [ ] **组织树等可复用组件提供 ref 支持**（使用 `forwardRef` + `useImperativeHandle`）

**表单规范检查**：
- [ ] **ProForm 使用 formRef 而非 Form.useForm()**
- [ ] **DrawerForm/ModalForm 设置 destroyOnHidden: true**
- [ ] **关闭时显式重置：formRef.resetFields() → setState → onOpenChange()**
- [ ] **编辑表单 useEffect 依赖项不包含 formRef**

### 代码审查检查

**静态检查**：
- [ ] **检查是否有硬编码颜色**
  ```bash
  # 在编辑器中使用正则搜索
  #[0-9a-fA-F]{3,6}
  rgb\(
  rgba\(
  ```

- [ ] **检查内联样式使用是否合理**
  ```bash
  # 搜索 style={{ 确认是否必要
  ```

**功能测试**：
- [ ] **验证主题切换效果**
  - 在亮色主题下测试
  - 在暗色主题下测试
  - 在紧凑模式下测试（如已支持）

- [ ] **测试多个页面**（防止级联影响）

### 提交前检查

**代码质量**：
- [ ] **运行代码检查工具**
  ```bash
  npm run lint      # ESLint + Stylelint
  npm run tsc       # TypeScript 类型检查
  ```

**清理工作**：
- [ ] **移除调试代码**（console.log、调试信息显示、未使用的样式）
- [ ] **强制刷新浏览器**（Ctrl+Shift+R）清除缓存验证

---

## 七、常见问题 FAQ

### Q1：什么时候可以使用内联样式？

**A**：仅在以下情况下使用：
1. 需要动态计算的位置属性（`left`、`top`、`width` 等）
2. 必须使用 JavaScript 计算的样式
3. 一次性使用的特殊样式

```typescript
// ✅ 合理的内联样式使用
<div style={{ left: `${position}px`, width: `${width}px` }}>
```

### Q2：CSS 变量和 token 有什么区别？

**A**：
- **CSS 变量**：在 Less/CSS 文件中使用，无类型检查
- **token**：在 TypeScript 中使用，有完整类型检查

```less
/* Less 文件中 */
.container {
  color: var(--ant-color-text);  /* CSS 变量 */
}
```

```typescript
/* TypeScript 文件中 */
const { token } = theme.useToken();
<div style={{ color: token.colorText }}>  {/* token 对象 */}
```

### Q3：如何调试主题相关问题？

**A**：
1. 使用浏览器开发工具查看计算后的样式
2. 检查 CSS 变量是否正确应用
3. 确认是否有 `!important` 覆盖了样式
4. 强制刷新浏览器清除缓存

### Q4：修改后其他页面出问题怎么办？

**A**：
1. **立即回退修改**（`git checkout`）
2. 不要试图通过更多修改来"修复"
3. 重新评估是否真的需要修改
4. 考虑使用非侵入性方案（Less 类而非组件 props）

### Q5：为什么表单提交后再次打开会残留数据？

**A**：确保三点：
1. ProForm 使用 `formRef` 而非 `Form.useForm()`
2. 设置 `drawerProps={{ destroyOnHidden: true }}`
3. 在 `onOpenChange` 中按顺序重置：formRef → state → callback

### Q6：Form.useForm() 和 formRef 有什么区别？

**A**：ProForm 系列组件必须用 `formRef`，原生 Form 用 `Form.useForm()`。ProForm 内部有额外的状态管理，使用 formRef 确保生命周期同步

---

## 八、页面统计功能开发计划

### 设计原则

**只使用专用统计接口，禁止基于当前页数据计算统计**

- ✅ 专用统计接口：查询全量数据，提供有意义的统计信息
- ❌ 当前页统计：仅统计当前分页数据，没有实际意义

### 已实现的页面统计

| 页面 | 路由 | 统计接口 | 统计内容 | 展示方式 |
|------|------|---------|---------|---------|
| **视频设备管理** | `/video-management/device` | `getDeviceStatistics` | 设备总数、在线、离线 | PageContainer header.extra + Tag |
| **IoT设备管理** | `/iot-management/device-management` | `getUnifiedDeviceStatistics` | 设备总数、在线、离线 | PageContainer header.extra + Tag |
| **事件数据查询** | `/event-management/event-data` | `getStatusStats` | 总数、待处理、处理中、已完成 | PageContainer header.extra + Tag |
| **可视化大屏** | `/visualization-dashboard` | `getUnifiedDeviceStatistics`<br/>`getPendingStats`<br/>`getDurationStats` | 设备统计、事件统计等 | 专用统计组件 + 图表卡片 |

### 其他可用的统计接口

以下接口暂未使用，可根据需求在未来添加：

| 接口名称 | 服务来源 | 适用场景 | 统计内容 |
|---------|---------|---------|---------|
| `getTrendStats` | eventhandler | 事件管理 | 时间维度的事件趋势 |
| `getNodeStats` | eventhandler | 事件管理 | 流程节点统计 |
| `getUserStats` | eventhandler | 事件管理 | 按处理人统计 |
| `getOrgStats` | eventhandler | 事件管理 | 按组织统计 |

### 开发注意事项

1. **必须使用专用统计接口**：不要基于表格当前页数据计算统计
2. **保持简洁**：优先使用 Tag 组件在 header.extra 展示，占用空间小
3. **性能优化**：统计接口调用应控制频率，避免频繁请求
4. **错误处理**：统计接口失败时应优雅降级，不影响主要功能
5. **主题适配**：Tag 颜色使用 Ant Design 预设值（blue/success/orange/processing等）

---

## 九、参考资源

### 官方文档

- [Ant Design 5 定制主题](https://ant.design/docs/react/customize-theme-cn)
- [Ant Design 5 Design Token](https://ant.design/docs/react/customize-theme-cn#seedtoken)
- [Ant Design Pro 官方文档](https://pro.ant.design/)
- [三种预设算法](https://ant-design.antgroup.com/docs/react/customize-theme-cn#使用预设算法)

### 相关文档

- `frontend/前端规范性分析报告.md`：当前不规范代码的详细分析
- `frontend/前端主题适配开发规范-精简版.md`：快速参考指南
- `frontend/Ant Design Pro 新手需知.md`：Ant Design Pro 开发指南

### 查询第三方库文档的注意事项

在使用第三方图表库（如 @ant-design/plots）时，务必注意：

1. **确认包名和版本**
   - 不同的包可能有不同的 API（如 `@ant-design/charts` ≠ `@ant-design/plots`）
   - 查看 `package.json` 确认确切的包名和版本号

2. **查找对应版本的文档**
   - 优先查看项目实际使用版本的文档
   - 避免使用过时或不匹配版本的示例

3. **文档获取方式**
   - 官方文档网站（注意是否为 SPA，可能需要查看源码）
   - GitHub 仓库的 examples 目录
   - TypeScript 类型定义文件（`.d.ts`）
   - 实际的示例项目（CodeSandbox、StackBlitz）

4. **验证 API 的正确性**
   - 在项目中小范围测试后再大面积应用
   - 遇到问题时查看控制台错误信息
   - 必要时查看源码或类型定义

**示例**：本项目使用 `@ant-design/plots@2.6.3`，饼图 label 的正确配置为：
```typescript
label={{
  text: 'fieldName',    // 指定显示的字段名
  position: 'spider',   // 使用引导线布局
  style: { ... }
}}
```

---

## 十、总结

### 预期收益

遵循本规范进行开发，您将获得以下收益：

| 指标 | 改进幅度 |
|-----|---------|
| 代码量 | 减少 **40-60%** |
| 可维护性 | 提升 **200-300%** |
| 调试效率 | 提升 **200%** |
| 主题适配成本 | 减少 **90%** |

### 记住

> **规范的代码可以自动适配主题，不规范的代码会让维护工作量增加数十倍甚至上百倍。**

> **对 ProComponents 组件的样式修改具有全局影响，必须格外谨慎。如果不确定，最好不要修改，让组件自动适配。**

---

**文档版本**：v7.8
**最后更新**：2025-01-26
**适用版本**：Ant Design 5.27.6 + Ant Design Pro 2.7.13

**主要变更**：
- v7.8: **完成页面统计功能实现** - 完成事件数据查询和IoT设备管理页面的统计功能实现，精简文档内容，移除已完成的待实现任务和冗长示例代码，将已实现页面统计改为表格形式展示
- v7.7: **新增页面统计功能开发计划** - 新增「八、页面统计功能开发计划」章节，明确统计功能开发原则（只使用专用统计接口），整理已实现和待实现的页面统计功能，提供简洁版实现示例（事件数据查询页面）
- v7.6: **表单规范修复完成** - 完成全部25个表单组件的规范化修复，所有表单均符合 ProForm 最佳实践；精简表单相关文档内容，删除冗余的代码示例和模板
- v7.5.1: **新增修复计划** - 完成项目现有表单组件排查，新增「表单规范修复计划」章节，包含：风险评估、待修复文件清单（按优先级分类）、修复模板、进度跟踪表
- v7.5: **新增表单规范** - 在「开发检查清单」和「常见问题 FAQ」章节新增 ProForm 表单规范，包括：formRef 使用、表单重置、destroyOnHidden 配置、状态管理等最佳实践；修复 LogicEngine/InfoAtomType 表单组件的数据残留问题
- v7.4: **重要优化** - 针对 1024x1366 小屏幕大幅调整参数：
  - 提高左侧面板最小阈值（350-380px）确保组织树不换行
  - 提高响应式断点到 xl (1600px) 和 xxl (2000px) 确保 1024px 保持水平布局
  - 配置应用导航栏在 < 1200px 时自动收起（breakpoint: 'xl'）
  - 更新三个场景的参数推荐值和断点选择指南
- v7.3: 优化 Splitter 参数推荐值，针对 1024x1366 等小屏幕提高最小阈值；调整响应式断点配置（lg/xl）；新增断点选择指南和屏幕分辨率参考
- v7.2: 完成高优先级页面 Splitter 重构（系统管理-组织管理、物联管理-设备管理、视频管理-设备管理）
- v7.1: 新增 Splitter 分隔面板重构计划，包含改造指南、待改造页面清单、参数推荐值和进度跟踪表
- v7.0: 重大更新 - 文档更名为「前端编码规范与最佳实践」，新增 Flex 组件迁移指南，扩展为全面的前端开发规范文档
- v6.1: 优化文档结构，将核心原则提前并合并，优化检查清单分类，删除重复内容
- v6.0: 精简文档，删除已完成任务清单，保留核心开发规范
- v5.0: 主题适配重构全部完成（主题切换、紧凑模式字体适配、新组件引入）
- v1.0: 建立前端主题适配开发规范

---
